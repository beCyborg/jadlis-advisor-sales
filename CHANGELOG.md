# Changelog — advisor-sales

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — [SemVer](https://semver.org/lang/ru/).
До 1.0.0 совет жил в плагине `advisors` 1.0.0–1.0.3 (репо `advisors`, теперь приватный источник `jadlis-advisors-source`).

## [Unreleased]

## [1.0.4] — 2026-09-22 — Переход на Opus 5.5 / Switch to Opus 5.5

### Для человека

- Совет целиком — советники, скептики и валидатор — теперь работает на новой модели Opus 5.5 с окном контекста в 1 млн токенов.
- Требует Claude Code ≥ 2.1.280: более старые версии могут не знать эту модель — сначала обновите Claude Code, потом совет.
- Effort, состав линз, формат вердикта и папка памяти не менялись.

### For agents

- Changed: `agents/advisor-opus.md` — `model: opus` → `model: claude-opus-5-5` (`effort: high` unchanged).
- Changed: `skills/advisor-sales/SKILL.md` — frontmatter `model: opus` → `model: claude-opus-5-5` (`effort: high` unchanged).
- Changed: `workflows/sales-council.js` — the `anchor-check` step: `model: 'opus'` → `model: 'claude-opus-5-5'` (`effort: 'low'` unchanged).
- Changed: `workflows/sales-write-pipeline.js`, `workflows/council-influence.js` — header comments: documented fallback `workerOpts: { model: 'claude-opus-5-5' }` (was `'opus'`); `council-influence.js` also names the worker pin Opus 5.5. Code path unchanged.
- Migration: requires Claude Code ≥ 2.1.280 — older versions may reject the unknown model ID `claude-opus-5-5` when loading the skill or spawning `advisor-sales:advisor-opus`; update Claude Code (`claude update`). The 1M context window is native to the model — no `[1m]` suffix. Callers that pass their own `workerOpts` are unaffected.

## [1.0.3] — 2026-09-08

### Для человека

- Обновление из источника jadlis-advisors-source.

### For agents

- Regenerated by `tools/split-councils.py`; see the source repo commit in `SHARED_FROM.txt`.

## [1.0.0] — 2026-09-07

### Для человека

- Первый релиз под именем `advisor-sales`: выделен из `advisors` 1.0.3 — репо на совет, команда `/advisor-sales`, ключ настройки `MEMORY_DIR`.

### For agents

- Generated by `tools/split-councils.py` of `jadlis-advisors-source`; worker agent `advisor-sales:advisor-opus`; `SHARED_FROM.txt` carries the source tag + content hash checked by CI.
